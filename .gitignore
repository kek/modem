.superpowers/
target/
Cargo.lock

# Android build artifacts (generated by scripts/build-android.sh).
# These are produced from Rust sources and Kotlin bindings on demand;
# Phase G will decide whether to commit them or build them in Gradle.
android/app/src/main/jniLibs/
android/app/src/main/java/uniffi/

# Android Gradle build/cache artifacts and local-only config.
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/*.iml
android/app/*.iml
captures/
